# AKS Upgrade Agent POC — Progress Tracker

> Living document. Updated whenever a meaningful change is implemented, validated, or troubleshot.
> Status reflects **actual verified state**, not intent. See status legend below.

**Status legend**
- 🟢 DONE — implemented AND validated/tested
- 🟡 IN PROGRESS — implementation exists or work is underway but validation is incomplete
- 🔴 NOT STARTED / MISSING — required work has not been implemented
- ⚠️ BLOCKED — work cannot proceed because of a known blocker

Last updated: 2026-08-31 (`aks_check_storage` correctness bug fixed and fully real-cluster validated: `storage_health` no longer reports HEALTHY while query_errors exist; cluster-wide pods/events queries fixed to use compact `-o jsonpath` rows instead of full `-o json`, still 1 Run Command call, 32.62s, 0 query_errors, real WARNING finding correctly detected. `aks_check_pod_health` output-size fix still awaiting real-cluster re-validation. Full suite: 64 tests passing.)

---

## 1. POC Objective

We inherited a partially-built "AKS Upgrade Agent": a Microsoft Foundry-hosted AI agent that talks to an MCP (Model Context Protocol) server, which in turn inspects a real, existing AKS cluster and reports on whether it is safe to upgrade. In plain terms: **before someone clicks "upgrade" on a production AKS cluster, this agent should be able to explain what will break and why** — unhealthy nodes/pods, PodDisruptionBudget conflicts, storage problems, deprecated Kubernetes APIs, etc. — and only allow a real upgrade to proceed once those risks are addressed or explicitly accepted.

We are **not** creating a new AKS cluster for this POC; we assess and (eventually) act on an existing one.

## 2. Architecture

### Current implementation (as of this assessment)
```
Foundry Agent (main.py, agent-framework-agent-with-foundry-toolbox-responses/src/...)
    ↓ (TOOLBOX_ENDPOINT / AKS_MCP_ENDPOINT env var, via toolbox.yaml)
AKS Operations MCP Server — TWO parallel entrypoints exist (architecture duplication, not yet resolved):
    - src/aks-operations-mcp/main.py        → intended for Azure Container Apps (azd host.containerapp)
    - src/aks-operations-mcp/function_app.py → legacy Azure Functions entrypoint (infra/main.bicep still provisions this by default)
    ↓ (azure-mgmt-containerservice + AKS Run Command API, using DefaultAzureCredential / user-assigned managed identity)
Existing AKS Cluster (not created by this project; referenced via `existingAksClusterResourceId`)
    ↓
kubectl (via AKS Run Command) → node/pod/pdb/storage JSON → classified into blockers/warnings
```

### Target architecture (per current direction)
```
Foundry Agent
    ↓
MCP Server (Azure Container Apps ONLY — Function App path to be retired)
    ↓
Managed Identity (user-assigned, dedicated to the Container App)
    ↓
Azure RBAC / AKS access (Reader + custom AKS Run Command role; write-path RBAC for real upgrades still unresolved)
    ↓
Existing AKS Cluster
```

**Key gap between current and target:** the Azure Functions hosting path (`function_app.py`, Function App/Storage/Plan/Log Analytics/App Insights in `infra/main.bicep`) is still fully provisioned by default (`deployAksMcpFunction = true`) even though the target is Container Apps-only. This has not yet been removed. See [Section 10](#10-infrastructure--deployment).

## 3. Phase Plan

| Phase | Status | Objective |
|---|---|---|
| 1 — Initialize Foundry Agent | 🟡 IN PROGRESS | Stand up the Foundry-hosted agent shell |
| 2 — Build AKS MCP Server | 🟡 IN PROGRESS | Implement AKS operational tools behind MCP (10/10 tools implemented; 1/10 fully validated end-to-end) |
| 3 — Connect to Existing AKS | 🟡 IN PROGRESS | Wire identity/RBAC to the existing cluster |
| 4 — Provision/Deployment | 🟡 IN PROGRESS | Bicep + azd provisioning of supporting resources |
| 5 — MCP Completion & Validation | 🟡 IN PROGRESS | All required tools implemented and tested |
| 6 — AKS Identity & RBAC Validation | 🟡 IN PROGRESS | Confirm least-privilege access actually works |
| 7 — Container Apps Deployment | 🟡 IN PROGRESS | MCP running remotely on Container Apps |
| 8 — Foundry ↔ MCP Integration | 🔴 NOT STARTED | Agent actually calling the deployed MCP endpoint |
| 9 — Pre-Upgrade Assessment | 🟡 IN PROGRESS | Composite readiness check across all risk areas |
| 10 — Intentional Failure Testing | 🔴 NOT STARTED | Demonstrate detection against real induced failures |
| 11 — End-to-End Validation | 🔴 NOT STARTED | Full path proven against the real cluster |
| 12 — Demo & Documentation | 🔴 NOT STARTED | Present the POC |

Detail per phase:

### Phase 1 — Initialize Foundry Agent
- **Status:** 🟡 IN PROGRESS (inherited, refined by us)
- **Objective:** Foundry-hosted agent shell that can call MCP tools.
- **Completed:** Agent scaffold (`main.py`) existed already (inherited). We replaced the generic placeholder instructions with an AKS Upgrade Agent persona (2026-08-30).
- **Remaining:** No confirmed live Foundry deployment/run of the agent in this environment; instructions don't yet reference all 9 available tools (e.g., storage, readiness, upgrade explicitly).

### Phase 2 — Build AKS MCP Server
- **Status:** 🟡 IN PROGRESS
- **Objective:** Expose AKS operational checks as MCP tools.
- **Completed:** 10 tools now implemented and registered identically in both `main.py` and `function_app.py`: `aks_get_cluster_details`, `aks_get_node_pools`, `aks_get_available_upgrades`, `aks_check_node_health`, `aks_check_pod_health`, `aks_check_pdb`, `aks_validate_upgrade_readiness`, `aks_upgrade_node_pool`, `aks_check_storage`, `aks_check_deprecated_apis` (added 2026-08-31). Real-cluster performance diagnosis (2026-08-31) surfaced bottlenecks in `aks_check_deprecated_apis` (17 calls, 546.12s — **fixed and fully validated, marked 🟢 DONE**: 1 call, 33.916661s, ~93.8% faster) and `aks_check_storage` (5 calls, ~159.14s namespace-scoped). `aks_check_storage` has now also been redesigned to use a single batched AKS Run Command (`tools/common.py`'s new `run_kubectl_batch`, same batching principle as the deprecated-API fix) — **implementation complete, unit tests written, but pytest execution and real-cluster re-validation have NOT yet occurred.**
- **Remaining:** `tools/discovery.py` and `tools/validation.py` still have no dedicated unit tests. `aks_check_pod_health`'s output-size problem (524,288-byte limit exceeded) remains unaddressed. `aks_check_storage`'s batching fix is unvalidated — do not mark it 🟢 DONE until both pytest and a real-cluster run (namespace `phonebook`) confirm it.

### Phase 3 — Connect to Existing AKS
- **Status:** 🟡 IN PROGRESS (inherited groundwork)
- **Objective:** Point the MCP server at the real, existing AKS cluster.
- **Completed:** `infra/main.bicep` takes `existingAksClusterResourceId` and never provisions a new cluster. Kubernetes RBAC bootstrap scripts (`bootstrap-k8s-rbac.ps1/sh`) exist to grant in-cluster read access.
- **Remaining:** No evidence in-repo that the bootstrap scripts were actually run against the real cluster, or that connectivity was confirmed end-to-end.

### Phase 4 — Provision/Deployment
- **Status:** 🟡 IN PROGRESS (inherited + modified)
- **Objective:** Provision supporting Azure resources.
- **Completed:** Bicep defines two user-assigned identities, RBAC modules, and (legacy) Function App hosting resources.
- **Remaining:** No captured evidence of a successful `azd up`/`azd provision` run in this repo. Legacy Function App resources still deploy by default and need a decision (remove vs. keep for now).

### Phase 5 — MCP Completion & Validation
- **Status:** 🟡 IN PROGRESS
- **Objective:** All required tools implemented and unit/integration tested.
- **Completed:** `aks_check_deprecated_apis` has full unit-test coverage AND real-cluster validation and is marked 🟢 DONE (see Section 7). `aks_check_storage` has been redesigned the same way (5 Run Command calls → 1, via the new shared `run_kubectl_batch` helper in `tools/common.py`); 8 new unit tests were added covering the batching/error-handling logic, and all 8 pre-existing storage classification tests were preserved unchanged.
- **Remaining:** `aks_check_storage`'s fix has **not** been executed via pytest this session, and has **not** been re-validated against the real cluster (`AKS_oldapp_cluster`, namespace `phonebook`) — do not mark it complete until both happen. No tests exist for `tools/discovery.py`, `tools/validation.py` (node/pod/pdb), or the `aks_upgrade_node_pool` write path. `aks_check_pod_health`'s output-size problem remains unaddressed.

### Phase 6 — AKS Identity & RBAC Validation
- **Status:** 🟡 IN PROGRESS
- **Objective:** Prove least-privilege identities actually work against the real cluster.
- **Completed:** Reader + custom "AKS Run Command" role (`Microsoft.ContainerService/managedClusters/runcommand/action` + `.../commandResults/read`) defined and assigned in Bicep for the Container App identity.
- **Remaining:** Write-path RBAC for `aks_upgrade_node_pool`'s real (non-dry-run) `agent_pools` write was intentionally left unresolved (exact ARM action not confidently verified — documented decision, not an oversight). No confirmation these role assignments were actually applied and exercised against the live cluster.

### Phase 7 — Container Apps Deployment
- **Status:** 🟡 IN PROGRESS
- **Objective:** MCP server running remotely on Azure Container Apps.
- **Completed:** `azure.yaml` defines `aks-mcp` as a `host.containerapp` service. A script (`configure-aks-mcp-container-identity.ps1`) exists to attach the dedicated managed identity to an **already-deployed** Container App.
- **Remaining:** No confirmation in this repo that the Container App has actually been deployed and is reachable; the identity-attach script assumes it already exists.

### Phase 8 — Foundry ↔ MCP Integration
- **Status:** 🔴 NOT STARTED
- **Objective:** The Foundry agent actually calls the deployed MCP tools live.
- **Completed:** `toolbox.yaml` has an `aks-mcp` entry wired up (no-auth pattern).
- **Remaining:** `server_url` is still the literal placeholder `<AKS_MCP_ENDPOINT>` — never replaced with a real endpoint. No evidence of a live toolbox creation (`azd ai toolbox create`) or a real agent-to-MCP call.

### Phase 9 — Pre-Upgrade Assessment
- **Status:** 🟡 IN PROGRESS
- **Objective:** One composite readiness check spanning all risk areas.
- **Completed (2026-08-30/31):** `aks_validate_upgrade_readiness` now aggregates node health, pod health, PDB health, storage health, and (new, 2026-08-31) deprecated/removed Kubernetes API findings, plus maintenance-window checks, into a single blockers/warnings result. When called from `aks_upgrade_node_pool`, the real requested `kubernetes_version` is now passed through as the deprecated-API check's target version (not invented/guessed).
- **Remaining:** Does not yet incorporate available-upgrade information (`aks_get_available_upgrades` is still a separate, uncorrelated tool call). No live-cluster validation of the combined readiness result.

### Phase 10 — Intentional Failure Testing
- **Status:** 🔴 NOT STARTED
- **Objective:** Prove detection using deliberately broken scenarios on the real cluster.
- **Completed:** Only synthetic/mocked unit tests exist (fabricated Python dicts, not real cluster manifests).
- **Remaining:** No real PDB-eviction-conflict, PV/PVC failure, deprecated-API, unhealthy-pod, or node-health scenario has been deployed to the actual cluster and observed by the agent.

### Phase 11 — End-to-End Validation
- **Status:** 🔴 NOT STARTED
- **Objective:** Full path (Foundry → MCP → real AKS → explained result) proven at least once.
- **Remaining:** Blocked on Phase 8 (real endpoint) and Phase 10 (real test scenarios).

### Phase 12 — Demo & Documentation
- **Status:** 🔴 NOT STARTED
- **Objective:** Present the POC with evidence.
- **Completed:** This tracking document is being created now.
- **Remaining:** Everything else.

## 4. Current Status

- **Current phase:** Phase 9 (Pre-Upgrade Assessment) is the most advanced. `aks_check_deprecated_apis` and `aks_check_storage` both have unit + real-cluster validation and are marked 🟢 DONE. `aks_check_pod_health`'s output-size fix is implemented and unit-tested (14 tests); real-cluster re-validation is the immediate next step.
- **Current task:** Real-cluster re-validation of `aks_check_pod_health` (unit tests already passing, 64 total in the full suite).
- **Overall POC status:** All 10 planned MCP tools are implemented. `aks_check_deprecated_apis` is **fully validated (🟢 DONE)**. `aks_check_storage` is now **also fully validated (🟢 DONE)**: a real-cluster run surfaced two distinct, evidence-based bugs which were both fixed and re-confirmed on the real cluster — (1) `storage_health` could report `HEALTHY` while `pods`/`events` query_errors were non-empty (fixed: `determine_storage_health` now takes `query_errors` and returns `INCOMPLETE` with the same BLOCKED > WARNING > INCOMPLETE > HEALTHY precedence already used by `aks_check_deprecated_apis`); (2) the cluster-wide batched query silently dropped `pods`/`events` from the combined output entirely (root cause: AKS Run Command's own output-size limit being hit by the *combined* response once full pod/event `-o json` was included cluster-wide — fixed by switching `pods`/`events` to compact `-o jsonpath` rows carrying only the fields the classification functions need, while `pvc`/`pv`/`storageclass` keep full `-o json` since they stay small). A follow-up jsonpath-escaping bug (`\n` interpreted as a literal newline by Python instead of the 2-character escape `\\n`, corrupting the shell-quoted argument) was also found via a real-cluster diagnostic run and fixed. Final confirmed real-cluster result: 1 Run Command call, 32.62s, **zero `query_errors`**, and a genuine `WARNING`-level finding correctly detected (a `FailedMount` event for a known pod in `deprecated-api-tests`). `aks_check_pod_health` previously ran cluster-wide `kubectl get pods -A -o json`; it has been redesigned to use a single compact `-o jsonpath` query (never `-o json`). Unit tests: **64 passed** (36 original + 14 pod-health + 14 storage-fix tests). Real-cluster validation of `aks_check_pod_health` specifically has **NOT** yet been performed.
- **What is working:** 10 MCP tools implemented; readiness folds in storage + deprecated-API + pod health; `aks_check_deprecated_apis` AND `aks_check_storage` both have full unit + real-cluster validation.
- **What is incomplete:** live endpoint wiring (Phase 8), real cluster test scenarios (Phase 10) for most risk areas, unit tests for `tools/discovery.py`, real-cluster validation for `aks_get_cluster_details`/`aks_get_node_pools`/`aks_get_available_upgrades`/`aks_check_node_health`/`aks_check_pdb`/`aks_upgrade_node_pool`/`aks_validate_upgrade_readiness`, and real-cluster confirmation of the new `aks_check_pod_health` implementation.
- **What we are doing next:** Run the exact real-AKS validation command (see [Section 15](#15-next-steps)) for `aks_check_pod_health` ONLY, record the actual output/timing, then decide whether the full `aks_validate_upgrade_readiness` real-cluster run is safe/fast enough to attempt.

## 5. Completed Work

| Date | Task | Description | Files changed | Validation performed | Result |
|---|---|---|---|---|---|
| 2026-08-30 | Phase 2 Task 1 | Added `aks-mcp` entry to `toolbox.yaml` (placeholder endpoint); replaced generic Foundry agent instructions with AKS Upgrade Agent persona | `toolbox.yaml`, `main.py` (Foundry agent) | Diagnostics only (`get_errors`); terminal was non-responsive this session | Implemented, not live-validated |
| 2026-08-30 | Phase 2 Task 2 | `function_app.py` now imports and exposes `aks_validate_upgrade_readiness`/`aks_upgrade_node_pool` from `tools/upgrade.py` (previously missing from the Function entrypoint) | `function_app.py` | Diagnostics only | Implemented, not live-validated |
| 2026-08-30 | Phase 2 Task 4 | Verified via Microsoft Learn docs that AKS Run Command needs a custom role (no built-in role covers it); added custom role Bicep + a new dedicated Container App identity; added identity-attach script with env-var-merge safety fix | `infra/modules/aks-run-command-role.bicep`, `infra/main.bicep`, `tools/common.py`, `scripts/configure-aks-mcp-container-identity.ps1` | Documentation review (Microsoft Learn); diagnostics only, no live Azure validation | Implemented, not deployed/validated |
| 2026-08-30 | Security review | Fixed a real bug: identity-attach script was replacing (not merging) Container App env vars, risking wiping unrelated settings | `scripts/configure-aks-mcp-container-identity.ps1` | Code review/trace only (no live container app to test against) | Fixed, not live-validated |
| 2026-08-30 | Phase 2 Task 5 | Implemented `aks_check_storage` (9th tool): PVC/PV/StorageClass/pod/event classification with context-aware severity rules | `tools/storage.py`, `main.py`, `function_app.py`, `tests/test_storage.py`, `tests/conftest.py`, `requirements.txt`, `README.md` | 8 unit tests written; execution not confirmed from this workstation (no Python here) — validated via manual line-by-line trace against implementation | Implemented; unit-test pass unconfirmed by direct execution in this session |
| 2026-08-30 | Phase 2 Task 6 | Integrated `aks_check_storage` into `aks_validate_upgrade_readiness` (storage blockers → readiness blockers, storage warnings → readiness warnings, only in `check_mode="full"`) | `tools/upgrade.py`, `tests/test_upgrade_readiness.py` | 5 new unit tests written (mocked node/pod/pdb, real storage classification functions); `get_errors` diagnostics clean; pytest not executed from this workstation | Implemented; unit tests written but execution not confirmed in this session |
| 2026-08-30 (VM) | Test execution | Commits `for testcases` and `updates from vm` show `test_upgrade_readiness.py` was added and Python `__pycache__` artifacts for `test_storage`/`test_upgrade_readiness`/`conftest` were generated using `pytest-9.1.1` (implies the suite was collected/run on the VM) | (bytecode only, no source changes in the second commit) | Indirect: presence of pytest bytecode cache implies execution occurred | **Not confirmed** — no captured console pass/fail output exists in the repo; cannot claim tests passed based on `__pycache__` alone |
| 2026-08-31 | Phase 5: `aks_check_deprecated_apis` | Implemented deprecated/removed Kubernetes API detection tool; integrated into `aks_validate_upgrade_readiness`; registered in both MCP entrypoints | `tools/deprecated_apis.py` (new), `tools/upgrade.py`, `main.py`, `function_app.py`, `README.md`, `tests/test_deprecated_apis.py` (new), `tests/test_upgrade_readiness.py` | `get_errors` diagnostics clean on all changed files; 8 new unit tests written for the tool + 1 new readiness-integration test; **pytest execution not possible from this workstation** (Python not installed — confirmed via Windows Store execution-alias error) | Implemented; validation incomplete at time of writing — see next row for the confirmed VM run |
| 2026-08-31 | Test execution (VM) | Full MCP unit test suite executed on the VM | `tests/test_storage.py`, `tests/test_upgrade_readiness.py`, `tests/test_deprecated_apis.py` (no code changes; test execution only) | **Actual pytest result: `22 passed in 4.12s`** — reported directly by the user after running the suite on the VM | All 22 unit tests pass (mocked/synthetic data only). **Real AKS cluster validation has NOT been performed.** Overall POC and end-to-end/real-cluster validation remain incomplete |
| 2026-08-31 | Real-cluster performance diagnosis | Measured real-cluster elapsed time for 5 tools plus estimated full readiness | No code changes; measurement only | **Reported by user, actual measurements against the real AKS cluster:** `aks_check_node_health` 4.83s; `aks_check_pod_health` 32.12s (cluster-wide output exceeded the 524,288-byte Run Command limit); `aks_check_pdb` 34.58s; `aks_check_storage` 159.14s (namespace-scoped); `aks_check_deprecated_apis` 546.12s (~9.1 min, 17 sequential Run Command calls); full readiness estimated ~13 minutes | Root cause identified: AKS Run Command has ~25-35s of per-invocation overhead; `aks_check_deprecated_apis`'s 17 sequential calls make it the dominant bottleneck. Recorded as the baseline for the optimization below |
| 2026-08-31 | Optimize `aks_check_deprecated_apis` (Run Command batching) | Redesigned the tool to check every relevant matrix entry in a **single** AKS Run Command invocation instead of one call per entry | `tools/common.py` (added `run_kubectl_raw` + internal `_execute_run_command` refactor, `run_kubectl_json` behavior unchanged), `tools/deprecated_apis.py` (rewritten `aks_check_deprecated_apis`, new `_build_batch_script`/`_parse_batch_output`/`_validate_namespace`), `tests/test_deprecated_apis.py` (rewritten: 13 tests) | `get_errors` diagnostics clean on all 3 changed/new files. Pytest was not executed at implementation time (see next rows for the confirmed real-cluster run) | Implemented |
| 2026-08-31 | Real-cluster performance validation of `aks_check_deprecated_apis` | Executed the optimized (batched) implementation against the real cluster `AKS_oldapp_cluster` and measured it against the pre-optimization baseline | No code changes; measurement/execution only | **Reported by user, actual measurements:** previous implementation 17 AKS Run Command calls / 546.12s; new implementation **1 AKS Run Command call / 34.3451432s (~34.35s)**; ~94% latency reduction. Confirmed executed against the real cluster (not mocked) | **Performance optimization validated and successful.** The 17→1 call reduction and ~94% latency improvement are confirmed on the real cluster |
| 2026-08-31 | Correctness bug found during the same real-cluster run | The real run returned multiple entries with `"API not available/servable on this cluster (kubectl exit code 1); usage could not be confirmed."` in `query_errors`, while `recommendations` simultaneously stated `"No deprecated or removed Kubernetes API usage detected."` | No code changes in this task (documentation-only; see [Section 12](#12-problems--blockers)) | Confirmed via the actual real-cluster tool output reported by the user | **Real bug identified, NOT fixed yet.** The tool can currently report a false "all clear" while several API checks were actually inconclusive. `aks_check_deprecated_apis` must NOT be marked fully 🟢 DONE until this is corrected |
| 2026-08-31 | Fix the deprecated-API correctness bug | `determine_deprecated_api_health` now takes `query_errors` and returns a new `"INCOMPLETE"` state (not `"HEALTHY"`) when there are no confirmed BLOCKER/WARNING findings but some entries could not be checked; a confirmed BLOCKER/WARNING still takes precedence over INCOMPLETE. The `"No deprecated or removed Kubernetes API usage detected"` recommendation is now only emitted when `blockers`, `warnings`, AND `query_errors` are all empty; a new `"Deprecated API assessment is incomplete because some API queries could not be executed."` recommendation is emitted whenever `query_errors` is non-empty (in addition to, not instead of, any blocker/warning recommendations) | `tools/deprecated_apis.py` (`determine_deprecated_api_health` signature + logic, recommendation-building block, docstrings); `tests/test_deprecated_apis.py` (updated/added 6 tests: no-usage recommendation assertion, INCOMPLETE health + no-misleading-recommendation on unavailable-only, new test for query_errors alongside real findings, extended `determine_deprecated_api_health` unit tests for the 3-arg form) | `get_errors` diagnostics clean. **Pytest was NOT executed this session** (attempt unavailable/skipped). **No real-cluster re-validation was performed** — the fix has only been manually traced against the new/updated test assertions, not executed | Implemented; **NOT yet validated by test execution or a real-cluster re-run — do not mark `aks_check_deprecated_apis` 🟢 DONE yet** |
| 2026-08-31 | Final validation of `aks_check_deprecated_apis` (unit + real cluster) | Full pytest suite executed; correctness fix re-run against the real cluster | `tests/test_storage.py`, `tests/test_upgrade_readiness.py`, `tests/test_deprecated_apis.py` (no code changes; test execution + live validation only) | **Reported by user, actual results:** unit tests **`28 passed in 1.78s`** (full suite, including all 14 deprecated-API tests and the correctness-regression tests). Real-cluster run against `AKS_oldapp_cluster` (subscription `bb0e2c9e-d7fb-45e4-92cf-654f380e6388`, resource group `AKS_Upgrade_agent`, Kubernetes `1.34.10`): `run_command_invocations: 1` (previous: 17), elapsed `33.916661s` (previous: `546.12s`), ~93.8% improvement. Final result: `deprecated_api_health: INCOMPLETE`, `findings: []`, `blockers: []`, `warnings: []`, `query_errors: 17`, recommendation correctly states "Deprecated API assessment is incomplete because some API queries could not be executed." | **Both performance and correctness CONFIRMED on the real cluster.** `INCOMPLETE` (not `HEALTHY`) is the correct, intentional result — on Kubernetes `1.34.10` the old API endpoints are no longer servable, so usage cannot be confirmed through them (not evidence of usage, not proof of absence). **`aks_check_deprecated_apis` is now marked 🟢 DONE.** |

## 6. Current Work

- **`aks_check_deprecated_apis` is now fully validated and marked 🟢 DONE:** unit tests `28 passed in 1.78s`; real-cluster run against `AKS_oldapp_cluster` confirmed `run_command_invocations: 1` at `33.916661s` (vs. the `546.12s`/17-call baseline, ~93.8% faster), and the correct `INCOMPLETE` result with `query_errors: 17` and the accurate incompleteness recommendation.
- No other code changes in this update — documentation only.
- Next: extend unit test coverage to `tools/discovery.py` and `tools/validation.py` (currently the largest test-coverage gap), then proceed toward Phase 8 (real MCP endpoint) wiring.

## 7. MCP Tool Inventory

| Tool | Purpose | Implementation Status | Validation Status |
|---|---|---|---|
| `aks_get_cluster_details` | Cluster metadata/current version | 🟢 DONE | 🔴 NOT TESTED (no unit tests; no live-cluster run confirmed) |
| `aks_get_node_pools` | Node pool inventory | 🟢 DONE | 🔴 NOT TESTED |
| `aks_get_available_upgrades` | Available K8s/node-image upgrade paths | 🟢 DONE | 🔴 NOT TESTED |
| `aks_check_node_health` | Node Ready/pressure conditions | 🟢 DONE | 🔴 NOT TESTED (only exercised via mocked stubs in readiness tests, not its own logic) |
| `aks_check_pod_health` | Pod phase/restarts/waiting reasons/scheduling/readiness | 🟢 DONE — redesigned to use a single compact `-o jsonpath` query (never `-o json`) instead of cluster-wide `-o json`, fixing the 512 KiB Run Command output-limit risk | 🟡 Unit tests passing (`tests/test_validation.py`, 14 tests, part of `50 passed in 1.35s`); 🔴 real AKS cluster validation not yet performed |
| `aks_check_pdb` | PodDisruptionBudget disruption risk | 🟢 DONE | 🔴 NOT TESTED |
| `aks_validate_upgrade_readiness` | Composite readiness check (node+pod+pdb+storage+maintenance window) | 🟢 DONE (storage now integrated) | � Unit tests passing (VM, `22 passed in 4.12s`, 2026-08-31, includes 6 readiness-integration tests); 🔴 real AKS cluster validation not yet performed |
| `aks_upgrade_node_pool` | Guarded node pool upgrade execution | 🟢 DONE (dry-run default, write gated) | 🔴 NOT TESTED (no unit tests exist for this function) |
| `aks_check_storage` | PVC/PV/StorageClass/pod/event storage health | 🟢 DONE — single Run Command; pods/events use compact `-o jsonpath` rows (never full `-o json`); `storage_health` correctly returns `INCOMPLETE` (never `HEALTHY`) when query_errors exist | 🟢 DONE — unit tests `64 passed` (full suite, incl. 24 storage tests); real-cluster validated against `AKS_oldapp_cluster`: 1 Run Command call, 32.62s, `query_errors: []`, correct `WARNING` result with a real `FailedMount` event finding (no truncation, no false HEALTHY) |
| `aks_check_deprecated_apis` | Detect deprecated/removed Kubernetes API usage vs. a target version | � DONE — Run Command batching (17 calls → 1) with a fixed, correctness-verified health/recommendation model | 🟢 DONE — unit tests `28 passed in 1.78s`; real-cluster validated against `AKS_oldapp_cluster` (Kubernetes `1.34.10`): 1 Run Command call, `33.916661s` (vs. `546.12s`/17 calls baseline, ~93.8% faster), correct `INCOMPLETE` result (`query_errors: 17`, no findings, no misleading "healthy" claim) |

## 8. AKS Upgrade Risk Coverage

| Risk | Detection Implemented | Tested | Notes |
|---|---|---|---|
| Node health | 🟢 | 🔴 | `aks_check_node_health`; no direct unit test |
| Pod health | 🟢 | � | `aks_check_pod_health`; 14 unit tests added 2026-08-31 covering healthy pods, Pending/CrashLoopBackOff/readiness-failure/scheduling-failure detection, multi-namespace, query failure, malformed rows, and compact single-invocation output shape; real-cluster validation not yet performed |
| PDB | 🟢 | 🔴 | `aks_check_pdb`; no direct unit test |
| PV/PVC | 🟢 | 🟢 | Classification logic unit-tested (`test_storage.py`); full suite `64 passed`; real-cluster validated against `AKS_oldapp_cluster` (2026-08-31): 1 PVC/1 PV, both Bound, no issues |
| Storage (mount/attach/provisioning failures) | 🟢 | 🟢 | Same as above; feeds into readiness (`test_upgrade_readiness.py`); real-cluster validated — correctly detected a real `FailedMount` event (git-repo volume plugin disabled) as a `WARNING`-level finding, `storage_health: WARNING`, `query_errors: []` |
| Deprecated/removed Kubernetes APIs | 🟢 | � | `aks_check_deprecated_apis`: fully validated \u2014 unit tests `28 passed in 1.78s`; real-cluster run against `AKS_oldapp_cluster` (Kubernetes `1.34.10`) confirmed 1 Run Command call, `33.916661s` (~93.8% faster than the 17-call/546.12s baseline), and the correct `INCOMPLETE` result (`query_errors: 17`, no findings) \u2014 the old API endpoints are unservable on this cluster's version, so usage cannot be confirmed through them (neither evidence of usage nor proof of absence) |
| Upgrade availability | 🟢 | 🔴 | `aks_get_available_upgrades`; not yet correlated with readiness output |
| Maintenance window | 🟢 | 🔴 | Implemented inline in `tools/upgrade.py`; no dedicated unit test |

## 9. Identity & RBAC

- **Managed identities (Bicep):**
  - `aksMcpIdentity` — original identity, attached to the legacy Function App.
  - `aksMcpContainerAppIdentity` — dedicated identity added for the Container App path (kept separate so Function RBAC wasn't touched).
- **Azure RBAC:**
  - Function identity: Reader + "Azure Kubernetes Service Cluster User Role" (built-in GUIDs), scoped to the existing AKS cluster resource.
  - Container App identity: Reader (built-in) + a **custom** role granting exactly `Microsoft.ContainerService/managedClusters/runcommand/action` and `.../commandResults/read` (verified against Microsoft Learn docs — no built-in role grants only these actions).
- **Kubernetes access:** `bootstrap-k8s-rbac.ps1/sh` scripts define a ClusterRole/ClusterRoleBinding for read-only access to nodes/pods/namespaces/PDBs — presence of the script is confirmed; actual execution against the real cluster is **not confirmed** in this repo.
- **AKS Run Command permissions:** Custom role covers only the run-command actions needed for read-only kubectl checks. Write permissions for real (non-dry-run) `aks_upgrade_node_pool` (`agent_pools` write action) are **intentionally not yet granted** — the exact ARM action string was not confidently verified from an authoritative source, so it was left unresolved rather than guessed. Real upgrades will fail authorization until this is verified and added.
- **Least privilege considerations:** Deliberately split identities per hosting path; custom role scoped narrowly instead of reusing a broad built-in role.
- **What has been tested:** Nothing — no `az role assignment list` output, no live run-command call, and no confirmation that either identity has actually been used against the real cluster from within this session.
- No secrets, tokens, or credentials are recorded in this document or the repo (confirmed via review).

## 10. Infrastructure / Deployment

- **Bicep (`infra/main.bicep`):** Defines both identities, both RBAC paths, and (still, by default) full Azure Functions hosting resources (Storage Account, Consumption Plan, Function App, Log Analytics, Application Insights) — this is the **legacy path** per the target architecture.
- **Azure Container Apps:** Declared in `azure.yaml` (`af-foundry-agent`, `aks-mcp` as `host.containerapp`), but **no Bicep persists the Container App resources themselves** — `azd` generates/manages them in memory (per `next-steps.md`). No confirmation a `azd up`/`azd provision` has been run and succeeded in this environment.
- **Azure Container Registry:** Not defined explicitly in Bicep; `remoteBuild: true` implies `azd` manages image build/push itself. Not independently verified.
- **Foundry resources:** Managed via `infra: provider: microsoft.foundry` in the nested `azure.yaml` — no persisted Bicep for this either; relies on `azd`'s Foundry provider.
- **MCP endpoint:** Real Container App URL is still an unresolved placeholder (`<AKS_MCP_ENDPOINT>`) in `toolbox.yaml`. The historically-referenced endpoint pattern (`https://aks-mcp.<env>.<region>.azurecontainerapps.io/mcp`) appears in scripts/docs but a live, current URL is not confirmed here.
- **Monitoring:** Log Analytics + Application Insights are wired **only** to the legacy Function App path. No monitoring is currently wired to the Container App path in Bicep.
- **Existing Function App code/infra still needing removal or replacement:** `function_app.py` (Azure Functions entrypoint) and the corresponding Bicep resources (`aksMcpFunctionStorage`, `aksMcpFunctionPlan`, `aksMcpFunctionApp`, `aksMcpLogAnalytics`, `aksMcpAppInsights`, `deployAksMcpFunction` parameter and its role assignment module) — these represent the architecture-duplication risk called out in Phase 1 audit and have not yet been removed.

## 11. Testing

| Test | Expected Result | Actual Result | Status |
|---|---|---|---|
| Healthy storage (Bound PVC + Bound PV + Running pod) | `HEALTHY`, no blockers | Unit test asserts this (`test_storage.py::test_healthy_storage`) | � confirmed passing (VM, `22 passed in 4.12s`, 2026-08-31) |
| Pending PVC referenced by an active pod | `BLOCKER` | Unit test asserts this (`test_storage.py::test_pending_pvc_has_clear_reason`) | 🟢 confirmed passing (VM, 2026-08-31) |
| Failed mount / volume attach | Detected as storage failure | Unit tests assert this (`test_failed_mount_is_detected`, `test_failed_volume_attachment_event_is_detected`) | 🟢 confirmed passing (VM, 2026-08-31) |
| Orphaned/unclaimed PV | `WARNING`, never a blocker | Unit test asserts this (`test_orphaned_pv_is_warning_not_blocker`) | 🟢 confirmed passing (VM, 2026-08-31) |
| Unrelated pod failure (ImagePullBackOff) | NOT classified as storage issue | Unit test asserts this (`test_unrelated_pod_failure_is_not_a_storage_problem`) | 🟢 confirmed passing (VM, 2026-08-31) |
| Storage blockers propagate into readiness | `aks_validate_upgrade_readiness` blocked | Unit test asserts this (`test_upgrade_readiness.py::test_pending_pvc_referenced_by_pod_blocks_readiness`, `test_failed_mount_blocks_readiness`) | 🟢 confirmed passing (VM, 2026-08-31) |
| Storage warnings do not block readiness | `is_ready=True`, warning present | Unit test asserts this (`test_orphaned_pv_warns_but_does_not_block_readiness`) | 🟢 confirmed passing (VM, 2026-08-31) |
| No deprecated APIs in use for target version | `HEALTHY`, no findings, 1 Run Command call | Unit test asserts this (`test_deprecated_apis.py::test_no_deprecated_apis_detected`); also asserts the "no usage detected" recommendation IS present when there are truly no findings/errors | � confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Deprecated-but-still-served API detected | `WARNING`, not blocked, count reported | Unit test asserts this (`test_deprecated_api_still_served_is_a_warning`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Removed-in-target API detected | `BLOCKER`, count reported | Unit test asserts this (`test_removed_api_in_target_is_a_blocker`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Multiple deprecated/removed API findings (single batched call) | All reported together, `run_command_invocations == 1` | Unit test asserts this (`test_multiple_findings_across_api_versions`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| API unavailable, no findings elsewhere → INCOMPLETE, not HEALTHY | Unavailable API surfaces as a query error; overall health is `INCOMPLETE`; "no usage detected" recommendation is NOT emitted | Unit test asserts this (`test_api_unavailable_is_distinguished_from_no_objects_found`, updated for the fix) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| API unavailable alongside a real BLOCKER/WARNING finding elsewhere | The real finding is still reported (BLOCKED/WARNING); `query_errors` and an "incomplete" recommendation are ALSO present, not hidden | Unit test asserts this (`test_query_errors_alongside_real_findings_preserve_both`, new) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Recommendation never claims "no usage detected" when query_errors is non-empty | Assertion embedded in the two tests above (no false "all clear" message) | Covered by `test_api_unavailable_is_distinguished_from_no_objects_found` and `test_query_errors_alongside_real_findings_preserve_both` | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Missing target version | Falls back to cluster's current version, no error | Unit test asserts this (`test_missing_target_version_falls_back_to_cluster_current_version`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Invalid target version format | Raises `ValueError` | Unit test asserts this (`test_invalid_target_version_raises`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Compact/batched response parsing | `{index: (exit_code, count)}` extracted correctly, incl. with surrounding banner noise | Unit tests assert this (`test_parse_batch_output_extracts_multiple_entries`, `test_parse_batch_output_ignores_surrounding_noise`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Batch script shape (single invocation, compact output only) | One script covers all relevant entries; requests `-o name`, never `-o json` | Unit test asserts this (`test_build_batch_script_uses_single_invocation_shape`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Invalid namespace rejected (injection hardening) | Raises `ValueError` for a non-RFC-1123 namespace value | Unit test asserts this (`test_invalid_namespace_raises`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| `determine_deprecated_api_health` 3-arg classification (incl. INCOMPLETE) | HEALTHY/WARNING/BLOCKED/INCOMPLETE all correctly derived from (blockers, warnings, query_errors) | Unit test asserts this (`test_determine_deprecated_api_health`, extended) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| Deprecated API findings propagate into readiness | Readiness blocked when a removed-API finding exists | Unit test asserts this (`test_upgrade_readiness.py::test_deprecated_api_findings_are_included_in_readiness`) | 🟢 confirmed passing (`28 passed in 1.78s`, 2026-08-31) |
| PDB disruption/eviction constraint (real cluster) | Detected as blocker | Not attempted | 🔴 NOT STARTED |
| Deprecated API usage (real cluster) — performance | 1 Run Command call, fast execution | **Actual: `run_command_invocations: 1`, `33.916661s` elapsed, vs. 17 calls/546.12s baseline (~93.8% faster). Executed against `AKS_oldapp_cluster` (Kubernetes `1.34.10`).** | 🟢 CONFIRMED — performance goal met |
| Deprecated API usage (real cluster) — correctness | Findings/recommendations accurately reflect what could and could not be confirmed | **Actual: `deprecated_api_health: INCOMPLETE`, `findings: []`, `blockers: []`, `warnings: []`, `query_errors: 17`; recommendation correctly states "Deprecated API assessment is incomplete because some API queries could not be executed."** | 🟢 CONFIRMED — `INCOMPLETE` is the correct, intentional result on this cluster (old API endpoints unservable on Kubernetes `1.34.10`; usage cannot be confirmed either way through them) |
| Healthy pods (all Running/Succeeded) | `pod_health_status: HEALTHY`, no unhealthy pods | Unit test asserts this (`test_validation.py::test_healthy_pods_report_healthy_status`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Pending pod / scheduling failure | Detected as unhealthy with `scheduled_reason` populated | Unit test asserts this (`test_pending_pod_is_unhealthy`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| CrashLoopBackOff on a `Running`-phase pod | Detected as unhealthy (phase alone is insufficient) | Unit test asserts this (`test_crashloopbackoff_running_pod_is_unhealthy`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Readiness-probe failure (Ready=False, no waiting reason) | Detected as unhealthy | Unit test asserts this (`test_readiness_failure_without_waiting_reason_is_unhealthy`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Multiple namespaces in one query | Correctly attributed per-namespace | Unit test asserts this (`test_multiple_namespaces_reported_correctly`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| kubectl query failure (non-zero exit) | `pod_health_status: INCOMPLETE`, never `HEALTHY` | Unit test asserts this (`test_kubectl_exit_failure_is_incomplete_not_healthy`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Run Command exception (no output at all) | `pod_health_status: INCOMPLETE`, error recorded, no raise | Unit test asserts this (`test_run_command_exception_is_incomplete_not_healthy`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Malformed output row | Recorded in `query_errors`, never silently dropped/healthy | Unit test asserts this (`test_malformed_row_is_recorded_as_query_error_not_hidden`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Confirmed unhealthy pod alongside a malformed row | `UNHEALTHY` takes precedence, malformed row still visible in `query_errors` | Unit test asserts this (`test_confirmed_unhealthy_pods_take_precedence_over_incomplete`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Pod-health script uses one invocation, never `-o json` | Compact `-o jsonpath`, single `kubectl` call | Unit test asserts this (`test_build_pod_health_script_uses_single_invocation_and_never_full_json`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Invalid namespace rejected (injection hardening) | Raises `ValueError` | Unit tests assert this (`test_invalid_namespace_raises`, `test_aks_check_pod_health_validates_namespace`) | 🟢 confirmed passing (`50 passed in 1.35s`, 2026-08-31) |
| Unhealthy pod (real cluster) | Detected as blocker | Not attempted | 🔴 NOT STARTED |
| Pod health output-size fix (real cluster) | Succeeds within Run Command's 512 KiB limit; correct timing and unhealthy-pod detection | Not yet attempted — see [Section 15](#15-next-steps) for the exact command to run | 🟡 AWAITING REAL-CLUSTER VALIDATION |
| Node health problem (real cluster) | Detected as blocker | Not attempted | 🔴 NOT STARTED |
| `determine_storage_health` 3-arg precedence (incl. INCOMPLETE) | BLOCKED > WARNING > INCOMPLETE > HEALTHY correctly derived from (blockers, warnings, query_errors) | Unit tests assert this (`test_determine_storage_health_query_errors_only_is_incomplete`, `test_determine_storage_health_blockers_and_query_errors_is_blocked`, `test_determine_storage_health_warnings_and_query_errors_is_warning`, `test_determine_storage_health_healthy_when_nothing_present`) | 🟢 confirmed passing (`64 passed`, 2026-08-31) |
| Missing pods/events batch sections reproduce as INCOMPLETE, never HEALTHY | `storage_health: INCOMPLETE`, both `pods`/`events` query_errors present, no "No storage issues detected" recommendation | Unit test asserts this (`test_aks_check_storage_missing_pods_and_events_reports_incomplete_not_healthy`) | 🟢 confirmed passing (`64 passed`, 2026-08-31) |
| Compact pod/event jsonpath row parsing (storage) | Rows converted back into the exact nested shape `find_pod_storage_failures`/`find_storage_events` expect; malformed rows counted, not silently dropped | Unit tests assert this (`test_parse_pod_storage_rows_compact_output_parsed_correctly`, `test_parse_pod_storage_rows_malformed_row_is_counted_not_silently_dropped`, `test_parse_event_storage_rows_compact_output_parsed_correctly`, `test_parse_event_storage_rows_malformed_row_is_counted_not_silently_dropped`) | 🟢 confirmed passing (`64 passed`, 2026-08-31) |
| Storage batch script requests compact jsonpath for pods/events, full json only for pvc/pv/storageclass, single invocation | `test_aks_check_storage_uses_a_single_batched_run_command` asserts script shape and call count | Unit test asserts this | 🟢 confirmed passing (`64 passed`, 2026-08-31) |
| Storage assessment (real cluster) — correctness after fix | `storage_health` reflects real findings, never a false HEALTHY when query_errors exist | **Actual (post-fix): `storage_health: WARNING`, `query_errors: []`, `blockers: []`, `warnings: ["Event for Pod/gitrepo-test-757c89444f-5tn5p in deprecated-api-tests: FailedMount - ..."]`. Executed against `AKS_oldapp_cluster`.** | 🟢 CONFIRMED — real WARNING-level storage finding correctly surfaced, no truncation, no query_errors |
| Storage assessment (real cluster) — performance | 1 Run Command call, ~30-40s | **Actual: `run_command_invocations: 1`, `32.616s` elapsed. Executed against `AKS_oldapp_cluster`.** | 🟢 CONFIRMED — performance goal met |

> Note: on 2026-08-31 the full MCP unit test suite (64 tests: deprecated-APIs 14 + storage 24 + readiness 6 + pod-health 14 + node/pdb none) was executed and reported `64 passed`. Combined with the real-cluster run above, `aks_check_storage` now also has full end-to-end validation (unit + real cluster), matching `aks_check_deprecated_apis`.

## 12. Problems / Blockers

| Date | Problem | Impact | Investigation | Resolution | Current Status |
|---|---|---|---|---|---|
| 2026-08-30 | `configure-aks-mcp-container-identity.ps1` used `az containerapp update --set-env-vars`, which replaces the entire env var list | Could silently wipe unrelated Container App configuration | Confirmed via known `az` CLI behavior | Rewrote script to fetch existing env vars, merge in the 4 managed vars by key, and pass the full merged list back | 🟢 Fixed (not live-validated against a real container app) |
| 2026-08-30 | Write-path RBAC action for `agent_pools` upgrades not confidently verified | Real (non-dry-run) node pool upgrades will fail authorization | Checked built-in role catalog; no authoritative source found for the exact write action in this session | Deliberately left unresolved rather than guessing, per explicit instruction | ⚠️ BLOCKED — needs an authoritative source before granting write RBAC |
| Ongoing | Python is not installed on this work PC | Cannot execute `pytest` or validate runtime behavior from this workstation | Confirmed via Windows Store execution-alias error when invoking `python` | User runs tests on a separate VM | ⚠️ BLOCKED (workstation-specific; not a code issue) |
| 2026-08-31 | `aks_check_storage`'s cluster-wide batched query silently dropped `pods`/`events` from the combined output entirely ("no result returned in the batched output"), while `storage_health` still reported `HEALTHY` | An operator/agent could read a cluster-wide storage check as clean when pod/event storage failures were never actually checked — same class of defect as the earlier `aks_check_deprecated_apis` bug | Real-cluster evidence: all-namespaces run had `pvc`/`pv`/`storageclass` populated but `pods`/`events` missing; the same namespace-scoped run succeeded cleanly for all 5 — pointing at AKS Run Command's combined-response output-size limit being hit once full pod/event JSON was included cluster-wide (pods/events were queried last and were the ones cut off) | Fixed two ways: (1) `determine_storage_health` now takes `query_errors` and returns `INCOMPLETE` (never `HEALTHY`) when no confirmed findings exist but some queries failed, mirroring `aks_check_deprecated_apis`; (2) `pods`/`events` now use compact `-o jsonpath` rows (only the fields `find_pod_storage_failures`/`find_storage_events`/`classify_pvcs` need) instead of full `-o json`, while `pvc`/`pv`/`storageclass` keep full `-o json` since they stay small — all 5 still issued in exactly ONE Run Command invocation. A follow-up real-cluster diagnostic then found a second, distinct bug in the new jsonpath templates: a Python string used `\n` (interpreted as a literal embedded newline) instead of `\\n` (the 2-character escape kubectl's jsonpath parser needs), which corrupted the shell-quoted argument and made kubectl exit 1 with "unterminated quoted string" — fixed by correcting the escaping | 🟢 RESOLVED — confirmed fixed by 64 passing unit tests AND a real-cluster re-run: 1 Run Command call, 32.62s, `query_errors: []`, correct `storage_health: WARNING` with a real `FailedMount` event finding. `aks_check_storage` marked 🟢 DONE |
| 2026-08-30 | Two parallel MCP hosting paths (`main.py` for Container Apps, `function_app.py` for Azure Functions) exist with historically different tool sets | Confusion about which is authoritative; doubles maintenance surface | Confirmed both now expose identical 9 tools, but legacy Function infra still provisions by default | Not yet resolved — pending decision to retire the Function App path | 🟡 Open |
| 2026-08-30 | `toolbox.yaml`'s `aks-mcp` entry has a literal placeholder `server_url` | Foundry agent cannot actually reach the MCP server until this is set | Confirmed via direct file read | Not yet resolved | 🔴 Open |
| Repo hygiene | `updates from vm` commit added compiled `__pycache__` bytecode files to source control | Repo noise; `.gitignore` only excludes `.azure` | Confirmed via `git show --stat` | Not yet fixed | 🟡 Open (minor, no functional impact) |
| 2026-08-31 | Real-cluster run of the optimized `aks_check_deprecated_apis` returned `query_errors` like `"API not available/servable on this cluster (kubectl exit code 1); usage could not be confirmed."` for multiple old API versions, while `recommendations` simultaneously stated `"No deprecated or removed Kubernetes API usage detected."` | **Misleading/contradictory output**: an operator or the Foundry agent could read this as "clean, no deprecated APIs" when in fact several checks were inconclusive (API unavailable/unqueryable), not confirmed-absent. This is a real correctness defect, not a false alarm | Root cause: the `recommendations` list appended the "no usage detected" message whenever `blockers` and `warnings` were both empty, WITHOUT checking whether `query_errors` was also non-empty; separately, `determine_deprecated_api_health()` computed `deprecated_api_health` from `blockers`/`warnings` alone and also did not factor in `query_errors`, so the overall health could read `HEALTHY` even when detection was incomplete | **FIXED IN CODE (2026-08-31):** `determine_deprecated_api_health` now accepts `query_errors` and returns a new `"INCOMPLETE"` state (instead of `"HEALTHY"`) when there are no confirmed findings but some entries could not be checked; a confirmed BLOCKER/WARNING still takes precedence. The "no usage detected" recommendation is now only emitted when blockers, warnings, AND query_errors are all empty. **CONFIRMED FIXED (2026-08-31):** unit tests `28 passed in 1.78s`; real-cluster re-run against `AKS_oldapp_cluster` (Kubernetes `1.34.10`) returned the correct `deprecated_api_health: INCOMPLETE` with `query_errors: 17` and the accurate incompleteness recommendation — no misleading "healthy"/"no usage" claim | 🟢 RESOLVED — confirmed fixed by both unit tests and a real-cluster re-run; `aks_check_deprecated_apis` marked 🟢 DONE |
| 2026-08-31 | Real-cluster performance diagnosis found `aks_check_deprecated_apis` took 546.12s (17 sequential AKS Run Command calls), `aks_check_pod_health` took 32.12s and exceeded the 524,288-byte Run Command output limit, and full readiness would take ~13 minutes | POC is impractically slow for interactive use; `aks_check_pod_health` risks truncated/corrupt output on cluster-wide queries | Root cause: AKS Run Command has ~25-35s of per-invocation overhead; `aks_check_deprecated_apis`'s 17 sequential calls made it the dominant cost | `aks_check_deprecated_apis` redesigned to use 1 batched Run Command instead of 17. **CONFIRMED on the real cluster (2026-08-31): 1 call, `33.916661s` vs. `546.12s` baseline, ~93.8% faster.** `aks_check_pod_health` redesigned (2026-08-31, this task) to use a single compact `-o jsonpath` query instead of `-o json` — implemented and unit-tested (50 passed), real-cluster re-validation still pending | 🟢 `aks_check_deprecated_apis` portion RESOLVED and confirmed on the real cluster; 🟡 `aks_check_pod_health` output-size fix implemented + unit-tested, awaiting real-cluster confirmation |

## 13. Technical Decisions

- **Existing AKS cluster will be used** — no new cluster is created by this POC; all Bicep/RBAC targets `existingAksClusterResourceId`.
- **MCP hosting direction is Azure Container Apps** — the Function App path is legacy/inherited and targeted for eventual removal, not further investment.
- **Managed Identity will be used instead of static credentials** — `DefaultAzureCredential`, optionally pinned via `AZURE_CLIENT_ID`, used throughout `tools/common.py`.
- **A dedicated identity was created for the Container App path** rather than reusing the Function App's identity, so RBAC changes for one path never risk affecting the other.
- **A custom RBAC role was created for AKS Run Command** rather than assigning a broader built-in role, after confirming no built-in role grants exactly the required actions (documented in Section 9).
- **Write-path RBAC for real upgrades is intentionally withheld** until the exact required ARM action can be verified from an authoritative source — "don't guess" was an explicit instruction.
- **Storage health detection lives only in `tools/storage.py`** — `aks_validate_upgrade_readiness` calls it and maps its output; no duplicate classification logic was added to `tools/upgrade.py`.
- **Existing upgrade safety gates are non-negotiable and were preserved as-is** through the storage integration: `dry_run=True` default, `check_mode='full'` required for writes, `AKS_UPGRADE_ENABLE_WRITE` env gate, approval-token check, and readiness-precheck-before-write.
- **POC will intentionally test PDB/PV-PVC/deprecated-API/unhealthy-pod/node-health failures** — currently only PV/PVC-style logic has any test coverage (synthetic, not live-cluster).
- **Real upgrade execution remains protected** until pre-upgrade assessment is proven reliable — enforced today via the existing gates listed above.
- **Deprecated-API detection uses targeted `kubectl get <plural>.<version>.<group>` queries against a small, explicitly-maintained matrix** (17 entries, sourced from the official Kubernetes deprecation guide) rather than an exhaustive historical compatibility database, and rather than trusting `kubectl api-resources` alone (which only reflects what the *current* server serves, not what will be removed by a future target version).
- **When `target_version` is not supplied to `aks_check_deprecated_apis`, it falls back to the cluster's own current `kubernetes_version`** (via `aks_get_cluster_details`) rather than inventing or guessing an upgrade target. When invoked internally from `aks_upgrade_node_pool` (via `aks_validate_upgrade_readiness`), the real requested `kubernetes_version` is passed through automatically, so the check is meaningful for that specific upgrade attempt without guessing.
- **`aks_check_deprecated_apis` was redesigned to issue ONE AKS Run Command invocation instead of 17** (2026-08-31), after a real-cluster performance diagnosis measured 546.12s / 17 sequential calls, driven by AKS Run Command's ~25-35s per-invocation overhead. Parallelizing the 17 calls was explicitly rejected as the primary fix (per instruction) in favor of batching them into a single remote script.
- **The batched script reports only compact counts per API version (`-o name` + count), never full object JSON** — this keeps output far below AKS Run Command's 524,288-byte limit and avoids the same truncation risk seen in `aks_check_pod_health`. The trade-off is losing per-object namespace/name detail in findings (only a count remains); classification accuracy (BLOCKER/WARNING) is unaffected.
- **Each batched entry's kubectl exit code is captured separately from its output** (via command substitution, not a plain pipe) specifically so an unavailable/removed API (non-zero exit) can never be misread as "zero objects found" (exit 0). This was a deliberate, tested distinction, not an assumption.
- **Added namespace input validation (`_validate_namespace`, RFC 1123 label format) to `aks_check_deprecated_apis`** as part of this change — the `namespace` parameter is interpolated into a shell command executed via AKS Run Command, and the new batched script is more complex than the previous single-`kubectl` invocation, so this closes a latent shell-injection risk for this tool specifically (identical interpolation exists unmodified in other tools' `-n {namespace}` usage, which was out of scope for this task).
- **An unavailable/unqueryable old API endpoint is treated as neither confirmation nor refutation of deprecated API usage.** Specifically: (1) we must NOT interpret an unavailable old API as evidence that deprecated API usage exists; (2) we must NOT interpret an unavailable old API as proof that no deprecated API usage exists; (3) the tool therefore reports `deprecated_api_health: "INCOMPLETE"` and preserves the specific `query_errors` so a human or the Foundry agent can see exactly which checks could not be confirmed, rather than collapsing that uncertainty into a false "HEALTHY" or an invented "BLOCKED". This was confirmed as the correct, intentional behavior via a real run against `AKS_oldapp_cluster` (Kubernetes `1.34.10`), where all 17 relevant matrix entries were unqueryable because those old API groups/versions are no longer served at all on that Kubernetes version.

## 14. Change Log

### 2026-08-30 — AKS upgrade MCP capabilities (Phase 2 Tasks 1, 2, 4, 5 + security fix)
**Phase:** 2, 4, 6
**Task:** Toolbox wiring, Function/Container tool parity, RBAC for AKS Run Command, `aks_check_storage`, env-var-merge bug fix
**Change:** Added `aks-mcp` entry to `toolbox.yaml`; rewrote Foundry agent instructions; synced `function_app.py` tool registry with `main.py`; added dedicated Container App managed identity + custom AKS Run Command RBAC role; implemented `aks_check_storage` (9th tool) with 8 unit tests; fixed an env-var-wipe bug in the identity-attach script
**Files:** `toolbox.yaml`, Foundry `main.py`, `function_app.py`, `infra/main.bicep`, `infra/modules/aks-run-command-role.bicep`, `tools/common.py`, `tools/storage.py`, `tests/test_storage.py`, `tests/conftest.py`, `requirements.txt`, `README.md`, `scripts/configure-aks-mcp-container-identity.ps1`
**Reason:** Complete Phase 2 tool set; establish least-privilege RBAC for the Container App path; prevent accidental config loss
**Validation:** Diagnostics (`get_errors`) clean; manual line-by-line trace of all 8 storage tests; no live Azure/pytest execution in this session
**Result:** Implemented, committed, pushed to `origin/main` (commit `ab9e242`)
**Next step:** Integrate storage into readiness (Task 6)

### 2026-08-30 — Integrate `aks_check_storage` into `aks_validate_upgrade_readiness` (Phase 2 Task 6)
**Phase:** 9
**Task:** Fold storage health into the composite readiness check
**Change:** `aks_validate_upgrade_readiness` now calls `aks_check_storage` inside the existing `check_mode="full"` branch; storage blockers/warnings are extended directly into readiness blockers/warnings; a new `storage_health` key was added to the returned dict (additive, no existing keys changed)
**Files:** `tools/upgrade.py`, `tests/test_upgrade_readiness.py`
**Reason:** POC objective requires PV/PVC/storage problems to factor into upgrade readiness, not just be reported standalone
**Validation:** `get_errors` diagnostics clean; 5 new unit tests written (mocked node/pod/pdb, real storage classification functions via `test_storage.py` fixtures); pytest execution not possible from this workstation (Python not installed)
**Result:** Implemented; safety gates on `aks_upgrade_node_pool` confirmed unchanged by direct file review
**Next step:** Confirm test execution on the VM; implement deprecated API detection tool

### 2026-08-30/31 (VM-side) — Test artifacts committed
**Phase:** 5
**Task:** Local test execution on VM
**Change:** `test_upgrade_readiness.py` committed (commit `9a362ef`); compiled `__pycache__` bytecode for `conftest`, `test_storage`, `test_upgrade_readiness`, and `tools/*` committed (commit `c338690`)
**Files:** (see commits `9a362ef`, `c338690`)
**Reason:** Presumed local validation pass on the VM where Python is installed
**Validation:** Not directly confirmed by us — no captured pytest console output exists in the repo; bytecode presence only proves the modules were imported/collected, not that all assertions passed
**Result:** Unconfirmed — treated as 🟡 IN PROGRESS, not 🟢 DONE, until actual pass/fail output is reviewed
**Next step:** Request/capture the actual pytest output from the VM run

### 2026-08-31 — Create POC-PROGRESS.md
**Phase:** 12
**Task:** Establish a living progress tracker distinct from `README.md`
**Change:** Added this document after inspecting the Foundry agent, both MCP entrypoints, all `tools/*.py` modules, `infra/main.bicep` and its modules, both `azure.yaml` files, `next-steps.md`, `README.md` files, `.gitignore`, and recent git history
**Files:** `POC-PROGRESS.md` (new)
**Reason:** Explicit request for chronological, factual tracking separate from general docs
**Validation:** Cross-checked every status claim against actual source code and git history (no assumptions about untested functionality marked DONE)
**Result:** Document created
**Next step:** See [Section 15](#15-next-steps)

### 2026-08-31 — Implement `aks_check_deprecated_apis` (Phase 5)
**Phase:** 5, 9
**Task:** Detect Kubernetes APIs/resources that are deprecated or removed relative to a target Kubernetes version, and fold findings into upgrade readiness
**Change:** New `tools/deprecated_apis.py` with a 17-entry, explicitly-documented deprecation matrix (group/version/kind, deprecated_in, removed_in, replacement — sourced from the official Kubernetes deprecation guide) and `aks_check_deprecated_apis(subscription_id, resource_group, cluster_name, target_version=None, namespace=None)`. For each matrix entry relevant to the target version, runs a targeted `kubectl get <plural>.<version>.<group>` query (via the existing `run_kubectl_json` helper — same managed-identity/AKS-Run-Command auth as every other tool, no new auth mechanism) to find real objects still using that API, then classifies each as `REMOVED_IN_TARGET` (BLOCKER) or `DEPRECATED_STILL_SERVED` (WARNING) using the project's existing severity vocabulary. If `target_version` is omitted, falls back to the cluster's own current version via `aks_get_cluster_details` (never invents a target). Registered as the 10th tool in both `main.py` and `function_app.py` (identical schemas). `aks_validate_upgrade_readiness` gained a new optional `target_kubernetes_version` parameter and now calls `aks_check_deprecated_apis` in `check_mode="full"`, extending its blockers/warnings exactly like the storage integration (no duplicate classification logic). `aks_upgrade_node_pool` now passes its real `kubernetes_version` through to readiness as the target, so the check is meaningful for actual upgrade attempts without guessing.
**Files:** `tools/deprecated_apis.py` (new), `tools/upgrade.py`, `main.py`, `function_app.py`, `README.md`, `tests/test_deprecated_apis.py` (new), `tests/test_upgrade_readiness.py`
**Reason:** Explicitly required POC objective ("a deprecated API checking tool is still required"); needed to determine target version without inventing one, and to keep detection accurate (usage-based, not just resource-type existence)
**Validation:** `get_errors` diagnostics clean on all 6 changed/new files; 8 new unit tests for the tool (no deprecated APIs / deprecated-still-served / removed-in-target / multiple findings / missing target version fallback / invalid target version / pure classify_entry / determine_deprecated_api_health) plus 1 new readiness-integration test, all using a monkeypatched synthetic matrix and mocked kubectl responses for determinism. **Pytest execution was attempted and failed** from this workstation — Python is not installed here (confirmed via Windows Store execution-alias error). No real AKS cluster was accessed or modified.
**Result:** Implemented; safety gates on `aks_upgrade_node_pool` (dry_run, check_mode, AKS_UPGRADE_ENABLE_WRITE, approval_token) confirmed unchanged by direct file review. No AKS upgrade, resource modification, or cluster creation was performed.
**Next step:** Obtain real pytest console output from the VM for the full suite (22 tests total across `test_storage.py`, `test_upgrade_readiness.py`, `test_deprecated_apis.py`) — see the following entry, where this was completed

### 2026-08-31 — VM pytest run confirmed: `22 passed in 4.12s`
**Phase:** 5, 9
**Task:** Execute the full MCP unit test suite on the VM (where Python is installed) and record the actual result
**Change:** No application code changes. The user ran the full test suite (`test_storage.py` + `test_upgrade_readiness.py` + `test_deprecated_apis.py`, 22 tests total) on the VM.
**Files:** None changed (test execution only); this update only touches `POC-PROGRESS.md`
**Reason:** Convert previously-"unconfirmed" unit test claims into a verified, recorded result, per the project's rule against claiming DONE without real validation
**Validation:** Actual pytest output reported: `22 passed in 4.12s`. This matches the true count of `def test_` functions across the three test files (8 in `test_storage.py` + 6 in `test_upgrade_readiness.py` + 8 in `test_deprecated_apis.py` = 22) — corrects an earlier miscount in this document that referred to "9" deprecated-API tests and "23" tests total.
**Result:** All 22 unit tests pass against mocked/synthetic data. This validates the classification and integration logic in isolation. **It does NOT constitute real AKS cluster validation** — no live cluster, Foundry agent, or deployed MCP endpoint was exercised. The overall POC and Phases 10/11 (intentional failure testing, end-to-end validation) remain incomplete.
**Next step:** Proceed to Phase 8 (real MCP endpoint wiring) and plan real-AKS-cluster validation (Phase 10/11)

### 2026-08-31 — Optimize `aks_check_deprecated_apis`: 17 Run Command calls → 1 (Phase 2/5)
**Phase:** 2, 5
**Task:** Fix the dominant performance bottleneck identified by a real-cluster diagnosis, without parallelizing the 17 calls
**Change:** Real-cluster measurements were reported: `aks_check_node_health` 4.83s, `aks_check_pod_health` 32.12s (output exceeded the 524,288-byte Run Command limit), `aks_check_pdb` 34.58s, `aks_check_storage` 159.14s (namespace-scoped), `aks_check_deprecated_apis` 546.12s / 17 sequential Run Command calls, full readiness ~13 minutes. Root cause: AKS Run Command's ~25-35s per-invocation overhead. `tools/common.py` gained a new `run_kubectl_raw` helper (via an internal `_execute_run_command` refactor that leaves `run_kubectl_json`'s behavior byte-for-byte unchanged). `tools/deprecated_apis.py`'s `aks_check_deprecated_apis` was rewritten to build one shell script (`_build_batch_script`) covering every relevant matrix entry, submit it as a SINGLE `run_kubectl_raw` call, and parse the compact response (`_parse_batch_output`) back into per-entry `(exit_code, count)` pairs. Each entry's kubectl exit code is captured via command substitution (never through a pipe, which would lose it) so an unavailable/removed API is never confused with zero objects found. Only compact `-o name` + count output is requested per entry - never full object JSON. Findings now report a `count` per API version instead of per-object namespace/name (disclosed trade-off; classification accuracy unchanged). Added `_validate_namespace` (RFC 1123 format check) since the batched script is more complex than the previous single-kubectl-call design. `classify_entry`, `determine_deprecated_api_health`, the 17-entry matrix, and target-version resolution (including the `aks_upgrade_node_pool` → `aks_validate_upgrade_readiness` pass-through) are all unchanged. The public function signature and MCP tool registration are unchanged.
**Files:** `tools/common.py`, `tools/deprecated_apis.py`, `tests/test_deprecated_apis.py` (rewritten: 13 tests)
**Reason:** Explicit task to fix the measured bottleneck via batching (not parallelization), while preserving classification behavior, target-version resolution, and the MCP interface
**Validation:** `get_errors` diagnostics clean on all 3 changed files. **Pytest was NOT executed in this session** (the attempt was skipped/unavailable in this environment). **Real-cluster validation was NOT performed** — no live AKS cluster, Foundry agent, or Container App was touched, and no before/after timing comparison exists yet. No AKS cluster, PDB, PVC, node pool, upgrade configuration, Container App, or Foundry integration was modified, per the explicit safety constraints.
**Result:** Implementation complete; **status is 🟡 IN PROGRESS, not 🟢 DONE**, per the explicit instruction not to mark this optimization complete without real validation. Expected outcome (unverified): 17 calls → 1 call; 546.12s → a small multiple of the ~25-35s per-invocation overhead instead of 17× it.
**Next step:** Execute the full pytest suite, then perform real-cluster validation of `aks_check_deprecated_apis` ONLY (per explicit scope) and record actual call count, elapsed time, result, and output size against the 17-call/546.12s baseline

### 2026-08-31 — Real-cluster validation of `aks_check_deprecated_apis`: performance confirmed, correctness bug found
**Phase:** 2, 5
**Task:** Validate the Run Command batching optimization against the real AKS cluster (`AKS_oldapp_cluster`), measuring only `aks_check_deprecated_apis` (not the full readiness assessment)
**Change:** No application code changes in this task — documentation only, per explicit instruction.
**Files:** `POC-PROGRESS.md` only
**Reason:** Confirm or refute the expected 17→1 Run Command reduction on a real cluster before marking the optimization complete
**Validation:** **Reported by user, actual measurements against the real cluster:** previous implementation 17 AKS Run Command calls / 546.12 seconds; new implementation **1 AKS Run Command call / 34.3451432 seconds (~34.35s)**; approximate latency reduction **94%**. The new implementation was successfully executed against the real cluster. **Separately, the same real run surfaced a correctness bug:** multiple old API versions returned `"API not available/servable on this cluster (kubectl exit code 1); usage could not be confirmed."` in `query_errors`, while the tool's `recommendations` simultaneously stated `"No deprecated or removed Kubernetes API usage detected."` — a contradictory, misleading result. Root cause: the "no usage detected" recommendation (and `determine_deprecated_api_health`'s `HEALTHY` result) are derived only from `blockers`/`warnings` and do not check whether `query_errors` is non-empty.
**Result:** **Performance optimization CONFIRMED SUCCESSFUL** (17→1 calls, ~94% latency reduction, executed against the real cluster). **`aks_check_deprecated_apis` is NOT fully DONE** — a real correctness bug (misleading "no findings" result when checks were actually inconclusive) must be reviewed and fixed first. No application code was changed to fix it in this task, per explicit instruction.
**Next step:** Design and implement a fix so `aks_check_deprecated_apis` never reports "no usage detected"/`HEALTHY` when `query_errors` is non-empty; re-run the unit test suite (still not executed this session) after the fix

### 2026-08-31 — Fix the deprecated-API correctness bug (Phase 2/5)
**Phase:** 2, 5
**Task:** Fix `aks_check_deprecated_apis` so it never reports "HEALTHY"/"no usage detected" while `query_errors` is non-empty, without automatically treating every unavailable API as a BLOCKER
**Change:** `determine_deprecated_api_health(blockers, warnings, query_errors=None)` gained a third parameter and now returns, in order of precedence: `"BLOCKED"` if any blocker exists, `"WARNING"` if any warning exists (even alongside query_errors), `"INCOMPLETE"` if there are no confirmed findings but some entries could not be checked, else `"HEALTHY"`. In `aks_check_deprecated_apis`, the recommendation-building block now only appends `"No deprecated or removed Kubernetes API usage detected for the target version."` when `blockers`, `warnings`, AND `query_errors` are ALL empty (previously it only checked blockers/warnings); a `"Deprecated API assessment is incomplete because some API queries could not be executed."` recommendation is appended whenever `query_errors` is non-empty, in addition to (not instead of) any blocker/warning recommendations. `classify_entry`, the 17-entry matrix, `_build_batch_script`, `_parse_batch_output`, target-version resolution, and the MCP tool signature/registration are all unchanged.
**Files:** `tools/deprecated_apis.py` (module docstring, `determine_deprecated_api_health`, `aks_check_deprecated_apis`'s recommendation logic and docstring), `tests/test_deprecated_apis.py` (updated 3 existing tests, added 1 new test `test_query_errors_alongside_real_findings_preserve_both`, extended `test_determine_deprecated_api_health` for the 3-arg form — now 14 tests total in this file)
**Reason:** Real-cluster validation exposed a case where several old API versions returned non-zero kubectl exit codes (API unavailable) while the tool simultaneously claimed a clean "no usage detected" result — a genuine correctness defect that could cause an upgrade to proceed on a false sense of safety
**Validation:** `get_errors` diagnostics clean. All new/updated test assertions were manually traced against the new logic (documented above) and are internally consistent. **Pytest was NOT executed this session** (attempt unavailable/skipped) and **no real-cluster re-run has been performed** — the fix is implemented but not yet confirmed by either test execution or a live cluster call.
**Result:** Fix implemented. **`aks_check_deprecated_apis` remains 🟡 IN PROGRESS, NOT 🟢 DONE**, per explicit instruction not to mark it done without both test and real-cluster confirmation. Performance result from the prior validation (1 call / ~34.35s vs. the 17-call/546.12s baseline) is unaffected by this change and still holds as the last confirmed measurement. No other MCP tool was modified.
**Next step:** Execute the full pytest suite (28 tests total), then re-run `aks_check_deprecated_apis` ONLY against `AKS_oldapp_cluster` (subscription `bb0e2c9e-d7fb-45e4-92cf-654f380e6388`, resource group `AKS_Upgrade_agent`) to confirm the fix and reconfirm the 1-call performance

### 2026-08-31 — `aks_check_deprecated_apis` validation complete: marked DONE
**Phase:** 2, 5
**Task:** Execute the full pytest suite and re-run `aks_check_deprecated_apis` against the real cluster to confirm the correctness fix and reconfirm performance
**Change:** No application code changes in this task — documentation only.
**Files:** `POC-PROGRESS.md` only
**Reason:** Convert the pending fix into a confirmed, evidence-backed DONE status, per the project's rule against claiming completion without real validation
**Validation:** **Unit tests:** full suite `28 passed in 1.78s` (storage 8 + readiness 6 + deprecated-APIs 14, including all correctness-regression tests). **Real AKS validation** against `AKS_oldapp_cluster` (subscription `bb0e2c9e-d7fb-45e4-92cf-654f380e6388`, resource group `AKS_Upgrade_agent`, current Kubernetes version `1.34.10`): `run_command_invocations: 1` (previous: 17), elapsed `33.916661s` (previous: `546.12s`), ~93.8% improvement. Final result: `deprecated_api_health: INCOMPLETE`, `findings: []`, `blockers: []`, `warnings: []`, `query_errors: 17`, with the recommendation correctly stating "Deprecated API assessment is incomplete because some API queries could not be executed."
**Result:** **`INCOMPLETE` is the correct, intentional final result** — not a bug. On Kubernetes `1.34.10`, the old (pre-removal) API endpoints this tool checks are no longer served by the API server at all, so usage cannot be confirmed through them. An unavailable old API must never be read as evidence that deprecated API usage exists, and must never be read as proof that no such usage exists — the tool correctly reports `INCOMPLETE` and preserves all 17 `query_errors` instead of collapsing that uncertainty into a false "HEALTHY" claim. Both performance (~93.8% faster, 1 call vs. 17) and correctness (accurate, non-misleading `INCOMPLETE` result) are now confirmed on the real cluster. **`aks_check_deprecated_apis` is marked 🟢 DONE.**
**Next step:** Add unit tests for `tools/discovery.py` and `tools/validation.py` (the next-largest test-coverage gap), then proceed toward Phase 8 (real MCP endpoint) wiring

### 2026-08-31 — Fix `aks_check_pod_health` output-size problem (Phase 2/5)
**Phase:** 2, 5, 9
**Task:** Fix `aks_check_pod_health` so cluster-wide queries reliably stay under AKS Run Command's 512 KiB (524,288-byte) output limit, without hiding failures or converting a query failure into a healthy/empty result
**Change:**
- **Old behavior:** `kubectl get pods -A -o json` returned FULL pod objects (env vars, volume mounts, resource requests/limits, labels/annotations, ownerReferences, etc.) for every pod cluster-wide, in a single Run Command call. Real-cluster measurement (prior session): 32.12s elapsed, with cluster-wide output at risk of exceeding the 524,288-byte limit and being silently truncated mid-object.
- **New architecture:** `tools/validation.py`'s `aks_check_pod_health` now issues a single AKS Run Command invocation using `kubectl get pods ... -o jsonpath='...'` (never `-o json`), emitting one compact `|`-delimited line per pod with only the fields needed for classification: namespace, name, phase, container restart counts, container waiting reasons, `PodScheduled` condition status/reason, and `Ready` condition status. This is still exactly ONE Run Command call — the fix targets per-pod payload size, not call count, per the same principle already used for `aks_check_deprecated_apis` (compact `-o name` counts) and `aks_check_storage` (batched `-o json` with `run_kubectl_batch`).
- **Classification (new/expanded):** a pod is unhealthy if phase is not `Running`/`Succeeded`, OR any container reports a waiting reason (covers `CrashLoopBackOff`, `ContainerCreating`, `ImagePullBackOff`/`ErrImagePull`, etc. generically, matching the pre-existing detection style — no reason strings are hardcoded), OR the pod is `Running` but its `Ready` condition is `False` (readiness-probe failures — new detection this task; the old implementation could not distinguish a "running but not ready" pod at all). `PodScheduled` status/reason is surfaced for scheduling-failure context on `Pending` pods.
- **Correctness / "do not hide failures":** a query failure (non-zero kubectl exit code, a Run Command exception, or malformed/short output rows) is never converted into "0 unhealthy pods" / healthy. It is recorded in a new `query_errors` list, and a new `pod_health_status` field reports `"INCOMPLETE"` (not `"HEALTHY"`) whenever no unhealthy pods were confirmed but the query could not be fully trusted — mirroring the same fix previously applied to `aks_check_deprecated_apis`'s health/recommendation model. A confirmed `UNHEALTHY` finding still takes precedence over `INCOMPLETE`.
- **Security:** added `_validate_namespace` (RFC 1123 label check) to `tools/validation.py`, scoped to `aks_check_pod_health` only (the `namespace` parameter is interpolated directly into a shell command run via AKS Run Command) — the identical gap in `aks_check_node_health`/`aks_check_pdb` was left untouched, per the explicit instruction not to change unrelated tools.
- **Output contract preserved:** `cluster_name`, `scope`, `total_pods`, `healthy_pods`, `unhealthy_pods` (with `namespace`/`name`/`phase`/`restart_count`/`waiting_reasons` per entry) are unchanged in shape; `unhealthy_pods` entries additionally now include `scheduled_reason`. New additive fields: `pod_health_status`, `query_errors`, `run_command_invocations`.
- **`tools/upgrade.py`:** `aks_validate_upgrade_readiness` now also appends a blocker ("Pod health could not be fully checked; query_errors present.") when `pod_health.get("query_errors")` is non-empty and no unhealthy pods were found — otherwise a pod-health query failure would silently pass through readiness as if 0 unhealthy pods meant healthy. This is the same category of fix already applied for storage/deprecated-API integration, scoped narrowly to the one new field being consumed.
**Files:** `tools/validation.py` (module docstring, `_validate_namespace`, `_build_pod_health_script`, `_parse_pod_health_output`, rewritten `aks_check_pod_health`), `tools/upgrade.py` (pod-health blocker branch), `tests/test_validation.py` (new, 14 tests)
**Reason:** Explicit task requirement — make `aks_check_pod_health` reliable enough for the full upgrade-readiness assessment to run against the real cluster without truncation risk, following the same minimal/batched-output principle already validated for the other two tools
**Validation:** `get_errors` diagnostics clean on all 3 changed/new files. **Full pytest suite executed this session: `50 passed in 1.35s`** (36 pre-existing + 14 new pod-health tests: healthy pods, Pending/scheduling-failure detection, `Running`-phase `CrashLoopBackOff` detection, `Running`-phase readiness-failure detection with no waiting reason, multi-namespace attribution, non-zero kubectl exit, Run Command exception, malformed output row, confirmed-unhealthy-takes-precedence-over-incomplete, single-invocation/no-`-o json` script shape, namespace-scoped script, jsonpath row parsing, and namespace-injection rejection). **Real AKS cluster validation has NOT been performed yet** — see [Section 15](#15-next-steps) for the exact command to run from the VM.
**Result:** Implemented and unit-tested; **status is 🟡 IMPLEMENTED/TESTED, NOT YET 🟢 DONE** — per project rule, real-cluster confirmation (timing, absence of truncation, correct unhealthy-pod detection) is required before this can be marked complete.
**Known limitations:** (1) `waiting_reasons`/`restart_count` are aggregated across all containers in a pod without per-container attribution (same simplification already accepted for `aks_check_deprecated_apis`'s count-only findings). (2) The custom-columns jsonpath approach embeds a literal `{"\n"}` inside a single-quoted shell string sent through AKS Run Command; this has been unit-tested for shape/parsing but not yet exercised against the real AKS Run Command shell. (3) `aks_check_node_health` and `aks_check_pdb` still lack the same `_validate_namespace` hardening applied here — intentionally out of scope for this task.
**Next step:** Run the exact real-AKS command in [Section 15](#15-next-steps) for `aks_check_pod_health` ONLY, report the actual output/timing back, then update this document with the confirmed result before attempting the full `aks_validate_upgrade_readiness` real-cluster run.

### 2026-08-31 — Fix `aks_check_storage`: correctness bug + real output-size bug, both found and fixed via real-cluster evidence
**Phase:** 2, 5, 9
**Task:** A real-cluster validation run (separate session) surfaced that `aks_check_storage`'s cluster-wide query returned `storage_health: HEALTHY` while `pods`/`events` `query_errors` were non-empty (missing from the batched output). Fix the correctness bug AND investigate/fix the root cause of the missing pods/events data, without reverting to multiple Run Command calls, per explicit task scope (storage only — `aks_check_pod_health`, `aks_check_deprecated_apis`, PDB, node health, and the real cluster's data were explicitly off-limits).
**Change:**
- **Bug 1 (correctness):** `determine_storage_health(blockers, warnings)` never considered `query_errors`, so a query failure could be reported as `"HEALTHY"`. Fixed: `determine_storage_health` now takes an optional third `query_errors` argument and returns, in precedence order, `BLOCKED` > `WARNING` > `INCOMPLETE` > `HEALTHY` — identical precedence to `determine_deprecated_api_health`. `aks_check_storage` now calls it with `query_errors` included. The existing recommendation logic (never emit "No storage issues detected" while `query_errors` is non-empty) was already correct and needed no change; its wording was tightened to "Storage assessment is incomplete because some storage checks could not be executed."
- **Bug 2 (root cause of missing pods/events):** Investigated using real-cluster evidence (not assumption): a namespace-scoped run of the exact same pre-fix code succeeded cleanly for all 5 queries, while the cluster-wide (`-A`) run had `pvc`/`pv`/`storageclass` intact but `pods`/`events` entirely absent from the combined batch output. This is consistent with AKS Run Command's own output-size limit being hit by the *combined* response once full pod/event `-o json` objects were included cluster-wide (`pods`/`events` are queried last in the script and were the ones cut off) — not a bug in the BEGIN/END marker parsing regex, which correctly parsed the sections that did survive. Fix: `pods` and `events` are now requested via compact `-o jsonpath` rows (`_POD_STORAGE_JSONPATH`/`_EVENT_STORAGE_JSONPATH`) carrying only the fields `find_pod_storage_failures`/`find_storage_events`/`classify_pvcs` actually read (namespace/name/phase/container-waiting-reason-and-message/PVC-claim-names for pods; namespace/involvedObject/reason/message/timestamps for events) — never full pod/event objects. `pvc`/`pv`/`storageclass` (small even cluster-wide) keep full `-o json`. All 5 queries remain in exactly ONE Run Command invocation via a self-contained script built directly in `tools/storage.py` (a new `_build_storage_batch_script`/`_parse_storage_batch_output`, mirroring the same BEGIN/END-marker-with-exit-code convention already used elsewhere in this codebase) — **`tools/common.py` was NOT modified**, since its generic `run_kubectl_batch` hardcodes `-o json` for every query and only `aks_check_storage` used it. The compact pod/event rows are converted back into the exact nested dict shape `find_pod_storage_failures`/`classify_pvcs`/`find_storage_events` already expect (via new `_parse_pod_storage_rows`/`_parse_event_storage_rows` + `_extract_pod_storage_rows`/`_extract_event_storage_rows`), so those classification functions — and every one of their pre-existing unit tests — are completely unchanged.
- **Bug 3 (found via a real-cluster diagnostic during Task 7 validation, not guessed):** after Bugs 1–2 were fixed, a real-cluster run returned `pods`/`events` query_errors of `"kubectl exited with code 1"` — no longer missing, but now failing outright. A targeted diagnostic (a standalone script that ran the same jsonpath query with `2>` captured to a file instead of `2>/dev/null`) showed kubectl's actual error: `"error parsing jsonpath ... unterminated quoted string"`. Root cause: the Python source used `'{"\n"}{end}'` (a *non-raw* string literal), which Python itself interprets as an embedded newline character at import time — so the literal newline, not the 2-character escape sequence `\n`, ended up inside the shell-quoted jsonpath argument, breaking kubectl's own string-literal parsing. Fixed by changing the source to `'{"\\n"}{end}'` (escaped backslash), producing the correct literal 2-character `\n` at runtime — matching how `tools/validation.py`'s `_POD_JSONPATH` (from the `aks_check_pod_health` fix) already did this correctly.
- **Output contract preserved:** all pre-existing return keys/shapes (`storage_health`, `pvcs`, `pvs`, `storage_classes`, `pod_storage_failures`, `storage_events`, `run_command_invocations`, `query_errors`, `blockers`, `warnings`, `recommendations`) are unchanged; only `storage_health`'s possible values gained `"INCOMPLETE"`.
**Files:** `tools/storage.py` (module docstring; `determine_storage_health` signature/logic; new `_POD_STORAGE_JSONPATH`/`_EVENT_STORAGE_JSONPATH`/`_STORAGE_BATCH_SECTION_RE`/`_build_storage_batch_script`/`_parse_storage_batch_output`/`_parse_pod_storage_rows`/`_parse_event_storage_rows`/`_extract_pod_storage_rows`/`_extract_event_storage_rows`; rewritten `aks_check_storage` to call `run_kubectl_raw` directly instead of `tools.common.run_kubectl_batch`); `tests/test_storage.py` (14 new tests; all 16 pre-existing tests preserved unchanged, including all classification-logic tests explicitly listed in this task's Task 4). **`tools/common.py`, `tools/validation.py`, `tools/deprecated_apis.py`, PDB logic, and node health were NOT touched.**
**Validation:** `python -m pytest tests/ -q` → **`64 passed`** (36 baseline + 14 pod-health + 14 storage-fix tests; run twice, once before and once after the Bug 3 fix). **Real AKS cluster validation performed** against `AKS_oldapp_cluster` (subscription `bb0e2c9e-d7fb-45e4-92cf-654f380e6388`, resource group `AKS_Upgrade_agent`), all-namespaces scope: first post-fix run (Bugs 1–2 only) returned `storage_health: INCOMPLETE` with `pods`/`events` `query_errors` of `"kubectl exited with code 1"` (progress — no longer silently missing/HEALTHY — but still failing, which led to finding Bug 3); after fixing Bug 3, the **final confirmed run** returned: `run_command_invocations: 1`, elapsed **32.616s**, `query_errors: []`, `storage_health: WARNING`, `blockers: []`, `warnings: ["Event for Pod/gitrepo-test-757c89444f-5tn5p in deprecated-api-tests: FailedMount - MountVolume.SetUp failed for volume \"git-source\" : git-repo volume plugin has been disabled; ..."]` — a genuine, correctly-detected storage-related event finding (the git-repo volume plugin is disabled on this cluster, matching the known lingering pod in `deprecated-api-tests` from earlier validation sessions).
**Result:** All three bugs confirmed fixed with real-cluster evidence (not just unit tests). `storage_health` never reports a false `HEALTHY` when query checks fail; cluster-wide `pods`/`events` queries no longer disappear from the batched response; the jsonpath queries themselves execute successfully against the real cluster. **`aks_check_storage` is marked 🟢 DONE.**
**Known limitations:** (1) Pod/event compact rows are pipe/caret/tilde-delimited — a waiting message or event message containing those literal characters could shift field boundaries (undetected in this real-cluster run; accepted trade-off, same category already accepted for `aks_check_deprecated_apis`'s count-only findings). (2) `aks_check_pod_health`'s own real-cluster re-validation (from the prior task) is unaffected by this change but is still the next open item.
**Next step:** Real-cluster validation of `aks_check_pod_health` (only tool in the readiness set still without a confirmed real-cluster run among the 3 recently-optimized tools), then decide whether the full `aks_validate_upgrade_readiness(check_mode='full')` is safe/fast enough to attempt.

## 15. Next Steps

1. **`aks_check_pod_health` real-cluster validation — DONE (confirmed in a separate validation-only session, all-namespaces, `AKS_oldapp_cluster`):** `run_command_invocations: 1`, elapsed `32.53s`, `query_errors: []`, `pod_health_status: "UNHEALTHY"` (2 of 30 pods correctly detected as unhealthy: `aks-command`'s own transient `ContainerCreating` helper pod, and the known lingering `ContainerCreating` pod `deprecated-api-tests/gitrepo-test-757c89444f-5tn5p`). No truncation. This, combined with `aks_check_deprecated_apis` and `aks_check_storage` (both above), means all 3 recently-optimized individual readiness checks now have confirmed real-cluster validation.
2. **Decide whether to attempt a full `aks_validate_upgrade_readiness(check_mode='full')` real-cluster run.** `aks_check_node_health` (4.83s) and `aks_check_pdb` (previously found `phonebook/webserver-pdb` with `disruptions_allowed=0` — a real, legitimate upgrade blocker, not a bug) were validated in earlier sessions but have no dedicated unit tests yet. Combined estimated readiness time is now roughly 4-5 sequential Run Command-backed checks × ~30-35s each (no longer ~13 minutes, since the two slowest checks were fixed), but this has not been measured end-to-end.
3. Add unit tests for `tools/discovery.py`, `aks_check_node_health`, and `aks_check_pdb` (currently the largest remaining test-coverage gaps).
4. Replace the `<AKS_MCP_ENDPOINT>` placeholder in `toolbox.yaml` with a real, deployed Container App URL once one is confirmed to exist and be reachable.
5. Decide on and execute removal/retirement of the legacy Azure Functions hosting path (`function_app.py` + related Bicep resources), or explicitly document why it's being kept.
6. Verify the exact ARM action required for `agent_pools` write operations and add the corresponding RBAC role before any real (non-dry-run) upgrade is attempted.
7. Confirm the Kubernetes RBAC bootstrap scripts have actually been run against the real cluster, and capture that evidence here.
7. Design and run at least one real intentional-failure scenario (start with PV/PVC or PDB, since those have the most existing logic) against the actual AKS cluster, and record the agent's real output.
8. Correlate `aks_get_available_upgrades` output into the `aks_validate_upgrade_readiness` result, so all risk areas are visible in one place.
9. Re-measure full readiness (`check_mode="full"`) end-to-end against the real cluster — previously estimated at ~13 minutes; now that `aks_check_deprecated_apis` alone dropped from 546.12s to 33.9s, the full readiness time should drop substantially, but this has not yet been measured.
10. Since `AKS_oldapp_cluster` is confirmed running Kubernetes `1.34.10` with all 17 legacy API versions already unservable, consider whether the deprecation matrix should be extended with more recent removals (e.g., anything removed after 1.26) to stay useful for clusters already this far ahead — optional, not urgent.
